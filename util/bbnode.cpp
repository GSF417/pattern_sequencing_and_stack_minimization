#include "util.h"

// this object represents a node in a search tree
class BBNode {
    private:
        int** cost; // dynamic array to represent the cost of each node in the search tree
        int** status; // dynamic array to represent the status of each node in the search tree
        int numObjects; // represents the number of objects we are working with
    public:
        BBNode() {

        }

        void print(int level) {
            int nodeCost;
            int nodeStatus;

        }
};