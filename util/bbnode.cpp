#include "util.h"

class BBNode {
    private:
        int** cost; // dynamic array to represent the cost of each node in the tree
        int** status; // dynamic array to represent the status of each node in the tree
        int numObjects; // represents the number of objects we are working with
    public:
        BBNode() {

        }

        void print(int level) {
            int nodeCost;
            int nodeStatus;

        }
};