#include "util.h"

class Set {
    private:
        int elements; // current number of elements this set holds
        int elements_max; // maximum number of elements this set may hold
        int* vector; // vector to represent the set
    public:
        Set() {

        }

};